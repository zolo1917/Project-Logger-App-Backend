server:
  port: 8003
spring:
  application:
    name: EventService
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://developdb-1.cebfuoekuf3x.ap-south-1.rds.amazonaws.com:3306/project_logger
    username: admin
    password: seshadri
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: ${spring.application.name}:${random.value}

